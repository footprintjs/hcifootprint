node_modules/
dist/
*.log
.DS_Store
.env
# The run's own tally, written by vitest's json reporter for the posttest badge
# gate (scripts/check-test-badge.mjs). Regenerated by every `npm test`.
.test-tally.json
