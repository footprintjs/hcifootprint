# build outputs — regenerated by `npm run build` (out/ is assembled into the
# Pages artifact by .github/workflows/pages.yml, never committed)
.next/
out/
# fumadocs-mdx generated collections (postinstall regenerates)
.source/
tsconfig.tsbuildinfo
