/**
 * Hand-written declarations for scripts/gen-fumadocs-api.mjs so the repo test
 * suite can import the pure helpers. Keep in lockstep with the .mjs.
 */
export declare function deriveTitle(md: string, fallback: string): string;
export declare function rewriteLinks(md: string): string;
